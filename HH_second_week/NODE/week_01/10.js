// 화살표 함수
// ES6 신 문법

function add (x, y) {
    return x + y
}

// 1-1 기본적인 화살표 함수
let arrowfunc01 = (x, y) => {
    return x + y
}

// 1-2 한 줄로
let arrowfunc02 = (x, y) => x + y

//
function testFunc(x) {
    return x
}

// 화살표로
let arrowfunc03 = x => x